
---

## 🧪 Описание заданий

### 🔹 Задание 01: Создание файловой структуры и валидация
**Цель:** Подготовка базовой файловой структуры для последующих лабораторных работ.

**Выполнено:**
- Разработаны скрипты `creators.sh`, `generators.sh`, `validators.sh` для автоматизированного создания директорий и файлов
- Реализована система валидации структуры (`validators.sh`)
- Добавлен модуль логирования (`logger.sh`) для фиксации этапов выполнения
- Главный скрипт `main.sh` объединяет все этапы в единую последовательность
- Результаты сохраняются в лог-файл `creation_log_*.txt`

**Навыки:** Работа с файловой системой Linux, bash-скриптинг, валидация данных.

---

### 🔹 Задание 02: Генерация тестовой файловой системы
**Цель:** Создание реалистичной файловой структуры для симуляции нагрузки на систему хранения.

**Выполнено:**
- Скрипты генерируют иерархию директорий с разным количеством вложенных уровней
- Создаются файлы различного размера и типов для имитации реальной нагрузки
- Фиксация процесса генерации в лог-файле `file_system_generator_*.log`
- Проверка целостности сгенерированной структуры

**Навыки:** Симуляция нагрузки на файловую систему, генерация тестовых данных.

---

### 🔹 Задание 03: Очистка и подготовка окружения
**Цель:** Разработка механизма безопасной очистки тестовых данных и подготовки чистого окружения для повторных запусков.

**Выполнено:**
- Скрипт `cleaners.sh` реализует безопасное удаление сгенерированных файлов и директорий
- Добавлены проверки перед удалением для предотвращения ошибок
- Утилиты `utils.sh` содержат вспомогательные функции для работы с путями и правами
- Логирование процесса очистки в `cleaner_*.log`
- Валидация успешности очистки через `validators.sh`

**Навыки:** Управление жизненным циклом тестовых данных, безопасная работа с файловой системой.

---

### 🔹 Задание 04: Генерация логов веб-сервера Nginx
**Цель:** Создание реалистичных логов веб-сервера для последующего анализа и мониторинга.

**Выполнено:**
- Генерация логов в формате Nginx (`access_2023-01-01.log`) с реалистичными записями:
  - IP-адреса клиентов
  - Временные метки
  - HTTP-методы и статусы
  - Размеры ответов
  - User-Agent
- Создание директорий `logs/` и `nginx_logs/` для хранения логов
- Модульное разделение логики: генерация (`generators.sh`), валидация (`validators.sh`), логирование (`logger.sh`)
- Поддержка генерации логов за произвольные периоды

**Навыки:** Работа с логами веб-серверов, симуляция трафика.

---

### 🔹 Задание 05: Анализ логов и извлечение метрик
**Цель:** Разработка скриптов для анализа логов Nginx и извлечения ключевых метрик для мониторинга.

**Выполнено:**
- Скрипт `analyzer.sh` парсит логи Nginx и извлекает:
  - Количество запросов по статус-кодам (200, 404, 500 и др.)
  - Топ запрашиваемых URL
  - Статистику по методам (GET, POST, PUT)
  - Распределение трафика по времени
- Интеграция с утилитами из `utils.sh` для обработки данных
- Результаты анализа сохраняются для последующей визуализации
- Автоматизация через `main.sh`

**Навыки:** Парсинг логов, извлечение метрик, подготовка данных для мониторинга.

---

### 🔹 Задание 06: Формирование HTML-отчётов
**Цель:** Создание наглядных отчётов о состоянии системы и результатах анализа.

**Выполнено:**
- Генерация интерактивного HTML-отчёта `report.html` с:
  - Сводной статистикой по метрикам
  - Визуализацией ключевых показателей
  - Графиками распределения запросов
  - Таблицами с детализацией
- Включение графиков и изображений (например, `local.jpg`)
- Скрипт `check.sh` выполняет финальную проверку данных перед формированием отчёта
- Адаптивная структура отчёта для удобного просмотра

**Навыки:** Генерация отчётов, HTML/CSS для визуализации данных.

---

### 🔹 Задание 07: Визуализация в Grafana + стресс-тестирование
**Цель:** Настройка дашбордов Grafana и проведение стресс-тестов для оценки производительности системы.

**Выполнено:**
- Настройка дашборда для мониторинга:
  - Загрузки CPU
  - Использования памяти
  - Дисковой активности
  - Сетевого трафика
- Проведение стресс-тестов с помощью утилит (stress, sysbench):
  - Нагрузка на CPU
  - Тестирование памяти
  - Дисковые операции I/O
- Фиксация результатов в скриншотах:
  - `grafana-server_and_prometheus.jpg.png` — обзорная панель
  - `stress_test.png` — результаты стресс-тестов
  - `cpu_disk_memory_node_file.png` — детальная метрика по ресурсам

**Навыки:** Работа с Grafana, настройка дашбордов, стресс-тестирование инфраструктуры.

---

### 🔹 Задание 08: Расширенные дашборды и сетевые тесты
**Цель:** Расширение системы мониторинга для анализа сетевой производительности и виртуальных окружений.

**Выполнено:**
- Настройка специализированных дашбордов:
  - Мониторинг виртуальных машин/контейнеров
  - Сетевая диагностика и анализ трафика
- Проведение сетевых тестов с помощью `iperf3`:
  - Измерение пропускной способности между узлами
  - Тестирование задержек и потерь пакетов
- Скриншоты результатов:
  - `dashboard.png` — основной дашборд
  - `dashboard_2_virtual.png` — мониторинг виртуальных окружений
  - `dashboard_iperf3_на_2_virtual.png` — результаты сетевых тестов

**Навыки:** Сетевой мониторинг, работа с виртуализацией, анализ сетевой производительности.

---

## 🛠️ Технологический стек

| Компонент | Назначение |
|-----------|------------|
| **Prometheus** | Сбор и хранение временных рядов метрик |
| **Grafana** | Визуализация метрик через дашборды |
| **Node Exporter** | Экспорт метрик хоста (CPU, RAM, Disk, Network) |
| **Nginx** | Веб-сервер для генерации и анализа логов |
| **stress / sysbench** | Инструменты для стресс-тестирования |
| **iperf3** | Тестирование сетевой производительности |
| **Bash** | Автоматизация всех этапов через скрипты |

---

## 📌 Примечания

> ⚠️ **Важно:** В репозитории представлены:
> - Скрипты автоматизации и анализа
> - Сгенерированные тестовые данные и логи
> - Скриншоты дашбордов и результатов тестов
> 
> Конфигурационные файлы Prometheus (`prometheus.yml`) и JSON-экспорты дашбордов Grafana не включены в репозиторий, но могут быть воссозданы на основе описанных в заданиях метрик и визуализаций.

---

## 🚀 Запуск проекта (рекомендации)

1. **Подготовка окружения:**
   ```bash
   cd 03 && ./main.sh  # Очистка предыдущих данных
   cd 01 && ./main.sh  # Создание структуры
